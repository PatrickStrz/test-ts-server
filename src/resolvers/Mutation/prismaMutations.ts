import { Context } from '../../utils'
const { forwardTo } = require('prisma-binding')

// 1-to-1 autogenerated mutations from prisma, based on datamodel.graphql
export const prismaMutations = {
    createPost: forwardTo('db'),
}
